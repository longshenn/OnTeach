<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onteach">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_tai"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnTeach">
        <activity android:name=".SelectAN"></activity>
        <activity android:name=".StoryAC" />
        <activity android:name=".SelectActivity_101"></activity>
        <activity android:name=".StoryActivity_101" />
        <activity android:name=".StoryAC" />
        <activity android:name=".SelectAC" />
        <activity android:name=".Start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>